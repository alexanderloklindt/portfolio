import React from 'react';

export interface ILoginPromptProps {}

const LoginPrompt: React.FunctionComponent<ILoginPromptProps> = (props) => {
  return (
    <div>
      <h2>Login prompt</h2>
    </div>
  );
};

export default LoginPrompt;
